# Delegation routine_builder-d1

- **Status:** done
- **Agent:** routine_builder
- **Server:** -
- **Risk limits:** -
- **Started:** -
- **Ended:** 2026-07-01T17:19:23.818906+00:00
- **Legacy session id:** routine_builder_1 (task id routine_builder-delegate-b1d710f8)

## Task

Create a routine for the agent `btc_usdt_okx_analyst` (agent_slug) that analyzes support and resistance levels for BTC-USDT on okx_perpetual. The routine should: fetch recent OHLCV candle data (4h and 1h), identify key support and resistance levels using swing highs/lows and volume clusters, label each level with strength (how many times it was tested), and return a clean structured output the agent can use to form a trade thesis. Attach the routine to the agent (strategy_id="btc_usdt_okx_analyst").

## Session

💭 **Reasoning**

> The user wants me to create a routine for the `btc_usdt_okx_analyst` agent that analyzes support and resistance levels for BTC-USDT on okx_perpetual. Let me follow the routine builder workflow:
> 
> 1. First, read the routine cookbook to understand patterns
> 2. Check existing routines
> 3. Create the routine
> 4. Test it
> 
> Let me start by reading the routine cookbook and checking existing routines in parallel.

💬 I'll start by reading the routine cookbook and checking existing routines in parallel.

🔧 **1. ToolSearch** (completed)

💭 **Reasoning**

> Now let me read the routine cookbook and check existing routines in parallel.

🔧 **2. mcp__condor__manage_skill** (completed)

🔧 **3. mcp__condor__manage_routines** (completed)

💭 **Reasoning**

> The routine cookbook skill wasn't found. Let me check if there are agent-local routines for the btc_usdt_okx_analyst agent, and also look at an existing routine to understand the pattern.

💬 No cookbook skill available, so let me pull a reference routine to understand the pattern, and check the agent's existing routines.

🔧 **4. mcp__condor__manage_routines** (completed)

🔧 **5. mcp__condor__manage_routines** (completed)

💭 **Reasoning**

> No agent-local routines exist yet for the `btc_usdt_okx_analyst` agent (the list returned global routines, not agent-local ones). I have a good reference routine (`ta_chart`) to understand the pattern.
> 
> Now let me look at the market_scanner routine to understand how candle fetching works, since it likely uses a similar pattern for multi-timeframe data.
> 
> Actually, I have enough from `ta_chart` to understand:
> 1. Uses `client.market_data.get_candles_last_days()` for candle data
> 2. Uses `ReportBuilder` for reports
> 3. Exports `Config` (Pydantic BaseModel) and `async def run(config, context) -> str`
> 4. Returns a `RoutineResult` for the final output
> 
> Now I'm planning the support/resistance routine. It needs to fetch 4h and 1h candles for BTC-USDT on okx_perpetual, then identify key levels using swing highs/lows (local price turning points) and volume clusters. Each level gets labeled with strength based on how many times it's been tested, and the output will be structured for trade thesis formation.
> 
> For the algorithm, a swing high occurs when a candle's high exceeds the highs of N neighboring candles on both sides, and a swing low follows the same logic for lows.
> 
> Writing the support/resistance routine... Writing configuration schema... Continuing the merge logic to combine nearby price levels by calculating a weighted average and consolidating their test counts and sources, then moving into the async run function to fetch candles from the server.
> 
> Fetching candles data... Now I'm extracting the current price from the latest 1h candle and identifying swing highs and lows from the 4h timeframe to build out resistance and support levels, storing each with its price, type, data source, and timestamp.
> 
> Building volume profile... Counting the number of tests for each resistance and support level, then merging nearby levels together and filtering out any that don't meet the minimum test threshold.  I'm classifying each level's strength based on how many times it's been tested, then identifying the nearest resistance and support levels to the current price.
> 
> Now I'm calculating the percentage distance from the current price to each level and building a formatted text summary that displays the resistance levels with their prices, distance percentages, strength classifications, test counts, and source indicators.
> 
> Adding support levels... Building the Plotly chart with candlestick data for the last 14 days and adding horizontal lines for resistance and support levels with color-coded strength indicators. Adding chart elements... Sending the chart to Telegram with a caption showing the count of resistance and support levels, then building a report with the BTC-USDT analysis including current price and level counts.
> 
> Now I'm reconsidering the volume profile visualization approach—the horizontal bar chart should probably be presented separately from the main candlestick chart rather than as a subplot. 't work when row 2 uses price as its X axis instead of time. I could use separate columns in the subplot layout instead, putting the candlestick chart in one column and the horizontal volume profile in another. But the simplest approach is probably just two rows: candlesticks with support/resistance lines on top, and regular volume bars below.
> 
> Actually, a side-by-side layout makes more sense for a volume profile—I'll create a two-column subplot where the candlestick takes up most of the width on the left and the volume profile sits narrower on the right, with their Y axes aligned so price levels match up.
> 
> Setting up the subplot layout... I see that `add_hline` spans across all rows and columns, so I need to use `add_shape` instead to draw support and resistance lines only in the first column. I'm also reconsidering the layout—maybe a simpler two-row approach with the volume profile displayed as vertical bars in the second row, where the y-axis represents price levels, and I can use `shared_xaxes=True` to keep the x-axes synchronized. I'll keep it simple: a 1-hour candlestick chart over the last 14 days with support and resistance levels as horizontal lines, color-coded by type, plus a traditional volume bar chart below it. The volume profile data is already embedded in the S/R levels themselves, so I don't need a separate visualization for that.
> 
> Now I need to verify that Plotly's `add_hline` method works correctly with subplot row and column specifications, then write out the final implementation. I should also check how the existing candle-fetching routine works to make sure I'm using the right API call.
> 
> Writing the chart code... Defining configuration fields... Defining helper functions to identify swing pivots by finding local highs and lows within a rolling window, then counting how many times price touches a support or resistance level within a tolerance range. Continuing the merge function and then defining a strength classifier that categorizes test counts into confidence levels. After that, I'm setting up the main async function to fetch market data across different timeframes.
> 
> Fetching candle data for both 4-hour and 1-hour intervals, parsing them into dataframes, and validating that both have data before proceeding. Then extracting the current price from the latest 1-hour candle and identifying swing pivot points from the 4-hour timeframe to build the analysis results. Distributing volume across price levels and filtering for high-volume nodes above the 75th percentile, then counting how many times each resistance and support level has been tested on the hourly data before merging nearby levels together. Formatting the output text with resistance and support levels, showing price, percentage distance from current, strength indicator, test count, and sources for each level. Writing chart visualization... Adding support levels... Finishing the chart configuration with axis labels, then rendering it as a PNG image and sending it to Telegram with details about the resistance and support levels found.
> 
> Now I'm building a report using a report builder, setting up metadata like the data source and tags, then populating it with key performance indicators showing the current price, number of resistance and support levels, and the nearest ones with their distances and trend directions.
> 
> Building the report visualization... to be safe, I'll skip the `annotation_font_color` parameter and just rely on the line color itself, since I'm not entirely certain about the annotation styling options in this context.
> 
> I also need to verify the import path for `RoutineResult` from the base module and confirm the correct `strategy_id` format when creating the routine—whether it should be the bare agent slug or a fully qualified path.
> 
> For the touch counting logic, I'm thinking through whether to use the same tolerance for both identifying touches and merging levels, or if I should use a smaller threshold for touch detection to be more precise about what counts as actually touching a level.
> 
> I should also make sure the parsing function handles empty DataFrames gracefully before I try to access any data, rather than checking afterward.
> 
> Writing the final implementation...

💬 Good, no agent-local routines exist yet for this agent. I have the `ta_chart` pattern to follow. Let me create the routine now.

🔧 **6. mcp__condor__manage_routines** (completed)

💭 **Reasoning**

> Routine created. Now let me test it.

💬 Created. Now testing:

🔧 **7. mcp__condor__manage_routines** (completed)

💭 **Reasoning**

> The routine ran successfully. Let me report the results to the user.

💬 Routine is working cleanly. Here's what was built and the live output:

---

**Routine: `btc_snr_levels`** — attached to `btc_usdt_okx_analyst`

**Live result (BTC @ $59,921.60):**

```
RESISTANCE
  $60,083  +0.27%  [MAJOR]  tests=181  src=vol_1h
  $60,332  +0.68%  [MAJOR]  tests=71   src=vol_1h
  $60,839  +1.53%  [MAJOR]  tests=43   src=swing_4h
  $62,586  +4.45%  [MAJOR]  tests=102  src=vol_1h
  $63,221  +5.51%  [MAJOR]  tests=36   src=swing_4h
  $64,081  +6.94%  [MAJOR]  tests=141  src=vol_1h
  $64,393  +7.46%  [MAJOR]  tests=120  src=swing_4h
  $64,745  +8.05%  [MAJOR]  tests=19   src=swing_4h

SUPPORT
  $59,755  -0.28%  [MAJOR]  tests=164  src=vol_1h
  $59,423  -0.83%  [MAJOR]  tests=119  src=vol_1h
  $59,125  -1.33%  [MAJOR]  tests=88   src=swing_4h+vol_1h
  $58,763  -1.93%  [MAJOR]  tests=67   src=swing_4h+vol_1h
  $58,032  -3.15%  [STRONG] tests=5    src=swing_4h

NEAREST RANGE
  R: $60,083  (+0.27%)  [MAJOR]
  S: $59,755  (-0.28%)  [MAJOR]
  Width: 0.55%  |  Position: 50.8% (mid-range)
```

**How it works:**
- **Swing pivots** — finds local highs/lows on 60d of 4h candles (±5 bar window), priced as candidate S/R
- **Volume clusters** — builds a 50-bin price histogram from 14d of 1h candles; top-25% volume nodes added as levels
- **Merge** — levels within 0.35% of each other are merged via weighted average
- **Touch count** — each merged level is scored by how many 1h candles touched it (±0.35%)
- **Strength** — MAJOR≥8, STRONG≥5, MODERATE≥3, WEAK<3 touches

The agent can call this routine to get a structured list of levels with distances and strength labels before forming a trade thesis.

## Result

I'll start by reading the routine cookbook and checking existing routines in parallel.No cookbook skill available, so let me pull a reference routine to understand the pattern, and check the agent's existing routines.Good, no agent-local routines exist yet for this agent. I have the `ta_chart` pattern to follow. Let me create the routine now.Created. Now testing:Routine is working cleanly. Here's what was built and the live output:

---

**Routine: `btc_snr_levels`** — attached to `btc_usdt_okx_analyst`

**Live result (BTC @ $59,921.60):**

```
RESISTANCE
  $60,083  +0.27%  [MAJOR]  tests=181  src=vol_1h
  $60,332  +0.68%  [MAJOR]  tests=71   src=vol_1h
  $60,839  +1.53%  [MAJOR]  tests=43   src=swing_4h
  $62,586  +4.45%  [MAJOR]  tests=102  src=vol_1h
  $63,221  +5.51%  [MAJOR]  tests=36   src=swing_4h
  $64,081  +6.94%  [MAJOR]  tests=141  src=vol_1h
  $64,393  +7.46%  [MAJOR]  tests=120  src=swing_4h
  $64,745  +8.05%  [MAJOR]  tests=19   src=swing_4h

SUPPORT
  $59,755  -0.28%  [MAJOR]  tests=164  src=vol_1h
  $59,423  -0.83%  [MAJOR]  tests=119  src=vol_1h
  $59,125  -1.33%  [MAJOR]  tests=88   src=swing_4h+vol_1h
  $58,763  -1.93%  [MAJOR]  tests=67   src=swing_4h+vol_1h
  $58,032  -3.15%  [STRONG] tests=5    src=swing_4h

NEAREST RANGE
  R: $60,083  (+0.27%)  [MAJOR]
  S: $59,755  (-0.28%)  [MAJOR]
  Width: 0.55%  |  Position: 50.8% (mid-range)
```

**How it works:**
- **Swing pivots** — finds local highs/lows on 60d of 4h candles (±5 bar window), priced as candidate S/R
- **Volume clusters** — builds a 50-bin price histogram from 14d of 1h candles; top-25% volume nodes added as levels
- **Merge** — levels within 0.35% of each other are merged via weighted average
- **Touch count** — each merged level is scored by how many 1h candles touched it (±0.35%)
- **Strength** — MAJOR≥8, STRONG≥5, MODERATE≥3, WEAK<3 touches

The agent can call this routine to get a structured list of levels with distances and strength labels before forming a trade thesis.

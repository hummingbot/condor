# Copy to .env beside docker-compose.yml. Both are required; compose refuses to
# start without them rather than defaulting to something guessable.
POSTGRES_PASSWORD=change-me
GRAFANA_PASSWORD=change-me

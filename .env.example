# ── Web Dashboard (optional) ─────────────────────────
# Full public URL for the dashboard (include port if needed). Loopback-only
# (§5.5) — no login. Also sets the server bind port.
# Examples: http://myserver.com:8088, https://dashboard.example.com
# WEB_URL=http://localhost:8088

